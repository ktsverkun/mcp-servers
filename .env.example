# === YClients MCP Server ===
# Партнёрский токен (обязательно) — получить в личном кабинете YCLIENTS
YCLIENTS_PARTNER_TOKEN=your_partner_token_here

# Пользовательский токен (опционально) — для операций с данными компании
YCLIENTS_USER_TOKEN=

# === Ozon MCP Server ===
# (нет обязательных токенов — сервер использует парсинг браузером)

# === Внутренние настройки (не менять) ===
MCP_TRANSPORT=http
MCP_HOST=0.0.0.0
MCP_PORT=8000
PORT=3001
NODE_ENV=production
